{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Debug app.py",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/app.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}